sudo: required
services:
    - docker

before_install: 
    - docker build -t vanessalofelt/docker-react -f Dockerfile.dev .

script:
    - docker run vanessalofelt/docker-react npm run test -- --coverage
    